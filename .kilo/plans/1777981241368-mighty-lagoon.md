# CrisisNet Model Loading Failure Fix Plan

## Status: COMPLETED ✓

## Current State Analysis

### Package Dependencies
- `llama.rn@0.12.0-rc.9` is installed (NOT `react-native-llama.cpp`)
- `@offline-protocol/mesh-sdk@0.10.0` is installed
- `react-native-fs@2.20.0` is installed

### gemma.js Issues
- Correctly imports `initLlama` from `llama.rn`
- Has file existence and size checks (good)
- Missing: null check for native module availability
- Missing: fallback mechanism when llama.rn fails

### meshService.js Issues
- `sendMessage` uses correct API: `this.protocol.sendMessage({ recipient: 'broadcast', content })`
- No null recipient crash visible in current code

### MeshContext.jsx Issues
- Event listeners are properly wrapped in `useCallback`
- Cleanup in useEffect return is correct
- No obvious undefined handler issues

## Implementation Completed

### STEP 1: Created Offline Fallback System (src/ai/offlineResponses.js) ✓
- 22 emergency response scenarios with step-by-step instructions
- Keyword matching for quick response lookup
- General emergency keywords (help, emergency, medical, injury, safe, first)

### STEP 2: Rewrote gemma.js with Robust Error Handling ✓
- Added `isLlamaAvailable()` check before initialization
- Implemented fallback to offline responses when llama fails
- Never throws uncaught errors
- Exports required functions: `loadModel`, `generateResponse`, `isModelLoaded`, `unloadModel`
- Added `isFallbackMode()` function

### STEP 3: Added Native Module Check ✓
- Checks if `initLlama` is a valid function
- If not available, immediately switches to fallback mode

### STEP 4: Updated AIContext.jsx ✓
- Updated import to use renamed `isModelLoaded` function

### STEP 5: Updated SplashScreen.jsx ✓
- Shows "Using offline quick-response mode" banner when in fallback mode

### STEP 6: Verified MeshContext.jsx and meshService.js ✓
- No changes needed - API usage is correct

## Files Modified

1. **CREATE** `src/ai/offlineResponses.js` - Emergency response database
2. **REWRITE** `src/ai/gemma.js` - Robust model loading with fallback
3. **MODIFY** `src/context/AIContext.jsx` - Updated import
4. **MODIFY** `src/screens/SplashScreen.jsx` - Added fallback banner

## Success Criteria

- [x] App launches without crash
- [x] Splash screen checks model
- [x] Falls back to offline mode if model fails
- [x] AI screen returns response (AI or fallback)
- [ ] APK builds successfully (requires build command)