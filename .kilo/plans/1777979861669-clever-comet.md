# CrisisNet Fixes Plan

## Status: COMPLETED ✓

## Overview
Fix 5 issues: duplicate function declaration, model loading progress, download progress, mesh provider crash, and slow UI during model load.

---

## FIX 1 — DUPLICATE FUNCTION DECLARATION

**File:** `src/utils/modelStorage.js`

**Issue:** Error mentions `downloadModel` declared at lines 69 and 73.

**Resolution:** No duplicate found in source. Only one `downloadModel` export at line 44. Error was from cached bundle.

---

## FIX 2 — MODEL LOADING PROGRESS BAR

**Files modified:**
1. `src/ai/gemma.js` - Added stage-by-stage progress with `setTimeout`
2. `src/context/AIContext.jsx` - Added `modelLoadProgress` state
3. `src/screens/SplashScreen.jsx` - Updated to show loading UI

**Changes made:**
- `gemma.js`: Fires progress at stages: Reading model file (10%), Loading into memory (30%), Initializing layers (60%), Warming up (85%), AI Ready (100%)
- `AIContext.jsx`: Added `modelLoadProgress` state, uses `InteractionManager.runAfterInteractions`
- `SplashScreen.jsx`: Shows logo, stage text, progress bar, percentage, and "This may take 30-60 seconds"

---

## FIX 3 — DOWNLOAD PROGRESS BAR

**File:** `src/screens/ModelDownloadScreen.jsx`

**Changes made:**
- Added download speed calculation (MB/s)
- Added ETA calculation (minutes and seconds)
- Added stall detection (10 seconds with no progress shows "Download stalled — check your connection")
- Added retry functionality

---

## FIX 4 — MESH PROVIDER CRASH

**File:** `src/context/MeshContext.jsx`

**Changes made:**
- Added `meshError` state
- Wrapped `meshService.init()` in try/catch
- Shows warning banner "Mesh unavailable — check Bluetooth permissions" when mesh fails
- App continues to work for AI features

---

## FIX 5 — SLOW UI DURING MODEL LOAD

**Files modified:**
1. `src/ai/gemma.js` - Added `setTimeout` between stages
2. `src/context/AIContext.jsx` - Uses `InteractionManager.runAfterInteractions`

**Changes made:**
- Model loading now yields to UI thread between stages
- Loading screen remains interactive